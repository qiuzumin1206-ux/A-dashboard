# Encrypted A-share dashboard

This public repository contains only an AES-256-GCM encrypted dashboard and
its browser-side unlock screen. It must never contain plaintext holdings,
account data, API keys, or the dashboard password.

The encrypted page is generated locally from the private `A` project. The
password remains in the local macOS keychain and is not stored in GitHub.

